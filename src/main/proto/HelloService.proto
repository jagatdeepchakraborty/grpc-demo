syntax = "proto3";
option java_multiple_files = true;
package com.grpc.gen;

message HelloRequest {
    string firstName = 1;
    string lastName = 2;
}

message HelloResponse {
    string greeting = 1;
    HelloRequest detail = 2;
}

service HelloService {
    rpc hello(HelloRequest) returns (HelloResponse);
}